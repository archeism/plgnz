# Kimi discovery probe outline

This is a protocol outline, not an executable reproduction script. The recorded
probe used a temporary KIMI_CODE_HOME, installed a local fixture through
POST /api/v1/plugins, read its skill/command counts and pointer, then shut down
the temporary server. No model request or normal-home deployment was made.
